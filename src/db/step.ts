export const getStepBySignature = async (signature: string) => {
  //   get the step from the database by signature
};

export const getStepById = async (id: string) => {
  //   get the step from the database by id
};
