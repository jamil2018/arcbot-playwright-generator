import { Template } from "@/models/Template";

export const addTemplate = async (template: Template) => {
  //add the template in the database
};

export const getTemplateById = async (id: string) => {
  //get the template from the database by id
};

export const getAllTemplates = async () => {
  //get all the templates from the database
};

export const updateTemplate = async (template: Template) => {
  //update the template in the database
};

export const deleteTemplate = async (id: string) => {
  //delete the template from the database
};
